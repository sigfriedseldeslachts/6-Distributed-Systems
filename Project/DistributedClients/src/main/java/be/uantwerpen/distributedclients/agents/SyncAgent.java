package be.uantwerpen.distributedclients.agents;

import jade.core.Agent;

import java.io.Serializable;

public class SyncAgent extends Agent implements Runnable, Serializable {

    // This Sync Agent stores a data structure (Agent’s list), i.e., a list of all files in the network

}
